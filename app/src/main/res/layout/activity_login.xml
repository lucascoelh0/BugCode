<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/home"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <include
            android:id="@+id/include_top_login"
            layout="@layout/top_block"
            android:layout_width="match_parent"
            android:layout_height="290dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_editor_absoluteX="10dp"
            tools:layout_editor_absoluteX="0dp" />

        <include
            android:id="@+id/include_body_login"
            layout="@layout/login_body"
            android:layout_width="match_parent"
            android:layout_height="450dp"
            android:layout_margin="10dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.829"
            tools:layout_editor_absoluteX="9dp" />

        <ImageView
            android:id="@+id/bug_logo"
            android:layout_width="250dp"
            android:layout_height="122dp"
            app:layout_constraintBottom_toTopOf="@+id/include_body_login"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/logo_bugcode" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>
