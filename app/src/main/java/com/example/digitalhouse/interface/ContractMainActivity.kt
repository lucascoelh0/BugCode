package com.example.digitalhouse.`interface`

import android.view.View
import android.widget.Button

interface ContractMainActivity {

    fun callFragDetailGastos()

    fun mudarCorHome(idCor: Int)

    fun mudarCorEntrada(idCor: Int)

    fun mudarCorGastos(idCor: Int)

}